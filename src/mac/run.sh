#!/bin/bash

# 첫 번째 명령어를 백그라운드로 실행
sudo python test.py 0 &

# 두 번째 명령어를 백그라운드로 실행
sudo python test.py 1 &

# 세 번째 명령어를 백그라운드로 실행
sudo python test.py 2 &

# 네 번째 명령어를 백그라운드로 실행
sudo python test.py 3 &

# 다섯 번째 명령어를 백그라운드로 실행
sudo python test.py 4 &

# 여섯 번째 명령어를 백그라운드로 실행
sudo python test.py 5 &

# 일곱 번째 명령어를 백그라운드로 실행
sudo python test.py 6 &

# 여덟 번째 명령어를 백그라운드로 실행
sudo python test.py 7 &
